<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SocietyConnect" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <style name="Theme.SocietyConnect.Splash" parent="Theme.SocietyConnect">
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:windowBackground">@drawable/bg_gradient_primary</item>
    </style>

    <!-- Card Style -->
    <style name="SocietyCard" parent="Widget.Material3.CardView.Elevated">
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="contentPadding">16dp</item>
    </style>

    <!-- Dashboard Feature Card -->
    <style name="DashboardCard" parent="Widget.Material3.CardView.Elevated">
        <item name="cardElevation">6dp</item>
        <item name="cardCornerRadius">20dp</item>
    </style>

    <!-- Button Styles -->
    <style name="PrimaryButton" parent="Widget.Material3.Button">
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="OutlineButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Text Styles -->
    <style name="TextTitle" parent="TextAppearance.Material3.HeadlineSmall">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextSubtitle" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">@color/text_secondary</item>
    </style>

    <!-- Status Chip Styles -->
    <style name="ChipPaid" parent="Widget.Material3.Chip.Assist">
        <item name="chipBackgroundColor">@color/tint_green</item>
        <item name="android:textColor">@color/status_paid</item>
    </style>

    <style name="ChipPending" parent="Widget.Material3.Chip.Assist">
        <item name="chipBackgroundColor">@color/tint_red</item>
        <item name="android:textColor">@color/status_pending</item>
    </style>

    <!-- Gradient Header Banner (Home, Auth screens) -->
    <style name="HeaderBanner">
        <item name="android:background">@drawable/bg_gradient_primary</item>
    </style>

</resources>
